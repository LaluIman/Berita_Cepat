import 'package:flutter/widgets.dart';

class LifestylenewsScreen extends StatelessWidget {
  const LifestylenewsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("LifeStyle"),
    );
  }
}